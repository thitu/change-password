#!/usr/bin/env bash

set -e

source .env

cert=$SSL_CERT
port=$SSL_PORT
key=$SSL_KEY

rails s \
  --environment=production \
  --log-to-stdout \
  --no-early-hints \  ## not everyone loves http/2
  --binding="ssl://0.0.0.0:$port?key=$key&cert=$cert"
